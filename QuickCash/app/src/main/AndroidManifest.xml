<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickCash"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".ui.Authentication.RegisterationActivity"
            android:exported="true"
            android:label="QuickCash">
            <!-- Set RegisterationActivity as the launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.Authentication.LoginActivity"
            android:exported="true"
            android:label="QuickCash" />

        <activity
            android:name=".ui.home.WelcomePage"
            android:exported="true"
            android:label="QuickCash" />

        <activity
            android:name=".ui.JobPosting.PostJobFragment"
            android:exported="true"
            android:label="Create a job">
        </activity>

    </application>

</manifest>
